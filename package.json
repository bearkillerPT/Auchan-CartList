{
  "name": "auchan_cart_to_json",
  "version": "1.0.0",
  "description": "A Scrapper that collect items added to the cart in auchan.pt's site and exports it to json",
  "main": "auchan_scrapper.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/chrome": "^0.0.180"
  }
}
